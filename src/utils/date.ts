/**
 * @description 日期格式化(时间戳转时间)
 * @param timeStamp 时间戳
 * @param isHours 是否带时分秒
 * @returns 格式化后的时间
 */
// export const timeStampToTimeStr = (timeStamp: number | string, isHours: boolean = false) => {};

/**
 * @description 日期格式化(时间转时间戳)
 * @param time 时间
 * @returns 转换后的时间戳
 */
// export const timeStrToTimeStamp = (time: string) => {};
